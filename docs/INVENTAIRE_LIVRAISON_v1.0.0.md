# Inventaire de livraison — v1.0.0

- 6 pages HTML publiques ;
- 1 feuille de styles publique et 3 scripts JavaScript ;
- 712 fichiers image publics, incluant les créations validées ;
- 1 fichier de contenu JSON public généré par release ;
- 92 fichiers applicatifs dans `cms/app` ;
- 13 migrations PostgreSQL ;
- 12 classes de tests plus le socle `TestCase` ;
- configurations Docker, Nginx de recette et cron ;
- documents fonctionnels, techniques, utilisateur, chiffrage, recette et exploitation ;
- taille de travail hors historique Git : environ 87 Mo avant ajout des preuves.

Ne sont volontairement pas livrés dans Git : `.env`, secrets SMTP, clé Laravel réelle, mot de passe PostgreSQL, stockage privé, sauvegardes, journaux, `vendor/` et `node_modules/`.
