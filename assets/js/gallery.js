
const box=document.createElement('div');box.className='lightbox';box.innerHTML='<button type="button" aria-label="Fermer">×</button><img alt="">';document.body.appendChild(box);document.querySelectorAll('[data-lightbox]').forEach(btn=>btn.addEventListener('click',()=>{box.querySelector('img').src=btn.dataset.lightbox;box.querySelector('img').alt=btn.dataset.title||'Création Faty Style';box.classList.add('open');}));box.addEventListener('click',e=>{if(e.target===box||e.target.tagName==='BUTTON')box.classList.remove('open');});
document.querySelectorAll('[data-carousel]').forEach(carousel=>{const track=carousel.querySelector('.carousel-track');carousel.querySelector('.next')?.addEventListener('click',()=>track.scrollBy({left:track.clientWidth*.8,behavior:'smooth'}));carousel.querySelector('.prev')?.addEventListener('click',()=>track.scrollBy({left:-track.clientWidth*.8,behavior:'smooth'}));let timer=setInterval(()=>track.scrollBy({left:track.clientWidth*.8,behavior:'smooth'}),5200);carousel.addEventListener('mouseenter',()=>clearInterval(timer));});
